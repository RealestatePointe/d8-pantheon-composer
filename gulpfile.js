var gulp = require('gulp'),
    sass = require('gulp-sass');

/**
 * Boilerplate
 * Run the starterkit from repmaster or define a custom theme task and
 * uncomment the default task below. The repmaster starterkit script
 * provides a theme task encapsulating its two default steps, compiling
 * sass and copying bootstrap's js libraries.
 */
// gulp.task('default', ['theme']);

